{
	"name": "tshirtwebsite",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"dev": "cd tshirt-website && npm run dev",
		"build": "cd tshirt-website && npm install && npm run build",
		"start": "cd tshirt-website && npm run start",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"description": "",
	"dependencies": {
		"@stripe/react-stripe-js": "^3.6.0",
		"@stripe/stripe-js": "^7.2.0",
		"stripe": "^18.0.0"
	}
}
